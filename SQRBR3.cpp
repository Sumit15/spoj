#include<iostream>
#include<cstdio>

using namespace std;

int fun(int x,int y)
{
    if(x==-1)
        return a[(n-y)/2];
    int p=
    if(!y)
        return 0;
    while(y>=0)
}

int main()
{
    int
    cin>>t;
    while(t--)
    {
        cin>>n>>k;
        for(i=0;i<k;i++)
            scanf("%d",&b[i]);
            b[k]=n;
        fun(k,0);
    }
    return 0;
}
