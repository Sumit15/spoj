#include<stdio.h>

int main()
{
    int
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        for(i=0;i<n;i++)
            scanf("%d",&a[i]);
        for()
    }
    return 0;
}
