#include<iostream>
#include<cstring>
#include<stdio.h>
{scanf("%d",&t);
while(t--)
    {
        scanf("%d%d",&n,&q);
        for(i=1;i<=n;i++)
            scanf("%d",a[i]);
        for(i=1;i<=q;i++)
        {

        }
    }
}
