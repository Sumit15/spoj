#include<iostream>
#include<cstdio>
#include<algorithm>

using namespace std;

int main()
{
    int t,a,b,gcd;
    cin>>t;
    for(q=1;q<=t;q++)
    {
        scanf("%*c%s",s);
        l=strlen(s);
        if(l<3);
            printf("Case %d: yes\n",q);
        a[0]=s[0];
        a[1]=s[1];
        //a[2]=s[2];
        for(i=2;i<l;i++)
        {
            a[k]=s[i];
            if(a[])
        }
    }
    return 0;
}

